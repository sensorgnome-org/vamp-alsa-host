#ifndef PARAMSET_HPP
#define PARAMSET_HPP

#include <map>
#include <string>
using namespace std;

typedef std::map < string, float > ParamSet; // parameter set for plugins
typedef ParamSet::iterator ParamSetIter; // iterator for ParamSet


#endif // PARAMSET_HPP
